{% extends 'base.html.twig' %}

{% block title %}restaurant{% endblock %}

{% block body %}
    <h1>restaurant : {{ restaurant.id }}</h1>
    <div>name :  {{ restaurant.name }}</div>
    <div>description:  {{ restaurant.description }}</div>
    <div>city name :  {{ restaurant.cityid.name}}</div>
    <div>date de création:  {{ restaurant.createdat|date('Y-m-d') }}</div>

{% endblock %}
